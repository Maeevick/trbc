import type { Position, GameState, Witch } from "./state.ts";
import { GRID_START, GRID_END, generateGrid } from "./grid.ts";

export {
  GRID_START,
  GRID_END,
  generateGrid,
  type Position,
  type GameState,
  type Witch,
};
